{
  "name": "truffle",
  "version": "0.0.1",
  "scripts": {
    "build": "npx truffle compile",
    "dev": "npx truffle migrate --network development",
    "start": "npx truffle migrate --network rinkeby"
  },
  "devDependencies": {
    "@truffle/hdwallet-provider": "1.2.2"
  }
}
